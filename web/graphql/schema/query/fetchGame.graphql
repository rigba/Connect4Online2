query FetchGame($gameId: String!) {
  fetchGame(gameId: $gameId) {
    id
    gameBoard
    whoseMove
    winner
    gameUUID
    createdId
    joinedID
    joined {
      id
      username
    }
    created {
      id
      username
    }
    rematch
  }
}