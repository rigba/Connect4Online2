subscription gameInfo($gameUUID: String!) {
  gameInfo(gameUUID: $gameUUID) {
    id
    gameBoard
    whoseMove
    winner
    joined {
      id
    }
    created {
      id
    }
    gameUUID
    createdId
    joinedID
  }
}