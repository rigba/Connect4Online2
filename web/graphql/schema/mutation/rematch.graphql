mutation Rematch($gameId: String!) {
  rematch(gameId: $gameId) {
    id
    gameBoard
    whoseMove
    winner
    gameUUID
    joined {
      id
      username
    }
    created {
      id
      username
    }
    createdId
    joinedID
    rematch
  }
}
